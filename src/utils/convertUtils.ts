export const checkNewLine = (text: string) => {
  return text.replaceAll('\n', '<br/>')
}

export const getHexByColor = (color: string) => {
  switch (color) {
    case 'blue':
      break

    default:
      break
  }
}
